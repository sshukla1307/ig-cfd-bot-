# IG CFD Trading Bot — Dependencies

# IG API (community library -- IG has no official first-party Python SDK)
trading-ig>=0.0.20

# LLM (both installed -- config.LLM_PROVIDER picks which one actually runs)
openai>=1.30.0
anthropic>=0.40.0

# Market data (technicals via continuous futures tickers, macro via FRED)
yfinance>=0.2.40
pandas>=2.2.0
fredapi>=0.5.0

# Utilities
python-dotenv>=1.0.0
httpx>=0.27.0
